# 2026-05-29 — 代码规范化 + bug修复

## 安装 Ruff 代码规范工具
- 安装 `ruff 0.15.15` 作为项目统一 linting + formatting 工具
- 在 `pyproject.toml` 中配置 `[tool.ruff]` / `[tool.ruff.lint]` / `[tool.ruff.format]`
- 启用的规则集: F/E/W/I/N/B/SIM/UP/C4/RUF/PERF (11组规则)
- 行宽 100，目标 Python 3.9

## 规范化修复
- **146 → 0 错误**：修复了全部 ruff 检测到的问题
- 主要修复类型:
  - `UP006/UP007`: `List[X]` → `list[X]`, `Optional[X]` → `X | None`
  - `I001`: 导入块排序
  - `E701`: 单行 `except:pass` 拆为两行
  - `SIM102`: 合并嵌套 if
  - `PERF401`: 循环 → 列表推导式
- 合理忽略的规则: RUF002 (中文全角标点), RUF003 (中文注释), N806 (轨道力学符号 M/E/N等)

## 严重 Bug 修复
1. **skyfield_backend.py dead code**: 删除 `create_satellite_from_kepler()` 中 return 后不可达的 25 行 TLE fallback 代码
2. **skyfield_backend.py 缩进bug**: `get_body_parameters()` 错误嵌套在 `_compute_topocentric()` 函数内部，现已移回 `SkyfieldOrbitBackend` 类中作为正确方法
3. **kepler_orbit.py F821**: 添加 `TYPE_CHECKING` 导入 `CelestialBody` 类型
4. **eccentricity_rad 误导**: 添加正确 docstring 说明偏心率无量纲

## 工程补充
- 创建 `README.md`：项目文档、API 总览、快速开始、架构说明
- 所有 `__init__.py` 添加 `__all__` 列表（显式声明公共API）
- 所有 `__init__.py` 重导出使用 `as` 别名

## 验证
- ruff check: All checks passed! (0 errors)
- 50+ 测试全部通过（含 Kepler + Skyfield 双后端）

---

## Step 2: 卫星设置开发 (同日)

### 新增模块
- `fl_space/orbit/satellite_config.py`: MultiClusterConfig, ClusterSpec, SatelliteSpec
- `fl_space/orbit/satellite_registry.py`: SatelliteRegistry + 全局 registry 单例
- `fl_space/viz/`: OrbitVisualizer + plot_dashboard + quick_plot

### OrbitSimulator 增强
- 新增 `orbits` 参数支持直接传入预构建轨道列表

### Demo 验证
- 5个 Demo 场景全部通过：双星簇、自定义注册、混合自定义卫星、4方对比、轨迹高亮
- 生成 5 张可视化图 (examples/output/)
- ruff 0 errors, 测试全部通过

### 技术要点
- GroundStation API: `lat_deg`/`lon_deg` (非 latitude_deg), `ground_stations[i]` (非 by_index)
- OrbitSimulator: `ground_station_network=` 参数, `stats['contact_rate']` 属性
- viz: `grid_stations=` 参数, 注意 `gs` 变量不与 GridSpec 的 `gs` 冲突
