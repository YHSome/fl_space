# 2026-06-01

## 代码规范文档创建
- 创建 `CODING_STANDARDS.md` — SpaceFL 项目完整的代码规范与开发指南
- 基于对项目所有源文件的深入分析，提取了实际使用的编码模式和约定
- **11个章节**：架构概述、命名规范、类型注解（Python 3.9+）、文档字符串（NumPy风格）、导入规范、数据模型模式、错误处理、Ruff代码质量工具、测试规范、AI辅助开发准则、提交前检查清单
- **AI辅助开发准则（第10章）** 包含标准提示词模板、8步检查流程、严禁事项列表
- 附录提供常见模式速查和常见错误示例

## Step 3: FL 算法模块完成

### 创建的文件 (fl_space/fl/)
- `core.py` — 四个核心抽象接口：ClientSelector, LocalTrainer, Aggregator, Evaluator + 数据结构（ClientState, ClientUpdate, FLRoundResult）
- `models.py` — MLP / SimpleCNN 示例模型 + 工厂函数 + 自定义模型注册机制
- `fedavg.py` — FedAvg 算法组件：RandomSelector + FixedEpochTrainer + SyncWeightedAggregator + StandardEvaluator + create_fedavg_components()
- `fedprox.py` — FedProx 算法组件：复用 selector/aggregator/evaluator，仅替换 ProximalTrainer（含proximal term μ·||w-w_global||²）
- `fedbuff.py` — FedBuff 异步算法：AsyncSelector + AsyncTrainer + BufferAggregator（FIFO缓冲区聚合，支持staleness降权）
- `scheduler.py` — 通信调度器：读取模拟器接触矩阵 → 通信窗口提取 → 连通性查询，完全独立于FL算法
- `server.py` — FLServer 编排器 + FLConfig 配置类：支持 run_sync()（FedAvg/FedProx）和 run_async()（FedBuff）双模式
- `config.py` — 预设配置：算法预设 + 规模预设（small/medium/large）+ 数据集预设（MNIST/CIFAR10）+ 6个组合预设
- `runner.py` — FLRunner 一键运行器：数据加载（IID/non-IID Dirichlet）→ 模型创建 → 训练执行
- `__init__.py` — 完整重导出 + __all__ 列表

### 设计要点
- **四组件解耦**：每个FL算法 = ClientSelector + LocalTrainer + Aggregator + Evaluator，组件可独立替换
- **算法间复用**：FedProx 只替换 trainer，FedBuff 替换 selector + aggregator，最大化代码复用
- **调度器独立**：完全遵循导师建议 — 通信判断与算法逻辑分离，接受OrbitSimulator引用输出连通性
- **遵循CODING_STANDARDS.md**：ruff check + ruff format 均通过，Python 3.9+ 类型注解，NumPy风格中文docstring
- **导入验证通过**：`import fl_space.fl` 成功，预设列表正常

## CLI 命令行工具完成 (2026-06-01)

### 文件变更
- 新增 `fl_space/cli.py` — 完整命令行工具（argparse，零额外依赖）
- 更新 `pyproject.toml` — 添加 `[project.scripts]` 注册 `fl-space` 命令
- 更新 `fl_space/__init__.py` — 添加 fl/ 层描述

### 五个子命令
| 命令 | 功能 | 典型用法 |
|------|------|---------|
| `fl-space simulate` | 轨道接触模拟 + 调参 | `--sats 10 --stations 5 --hours 24 --backend kepler` |
| `fl-space train` | FL 训练实验 | `--algo fedprox --dataset cifar10 --rounds 100 --device cuda` |
| `fl-space list` | 查看内置资源 | `presets|models|satellites|experiments` |
| `fl-space export` | 导出模拟结果JSON | `--body mars --sats 5 --output mars.json` |
| `fl-space info` | 系统环境信息 | (无参数) |

### 调参覆盖
- **simulate**: 卫星数、地面站数、时长、后端、轨道高度、倾角、分布策略、时间粒度
- **train**: 算法(fedavg/fedprox/fedbuff)、数据集、规模、轮次、epoch、学习率、batch size、μ(FedProx)、缓冲K(FedBuff)、staleness降权、IID/non-IID、α(Dirichlet)
- **全局**: --output JSON导出、--quiet 安静模式

## JSON 配置加载 + CLI 文档 (2026-06-01)

### 新增功能
- `FLConfig.from_dict()` / `FLConfig.from_json()` / `FLConfig.to_dict()` / `FLConfig.to_json()` — 完整序列化支持
- CLI 所有子命令增加 `--config` / `-c` 参数（JSON → CLI 覆盖合并）
- CLI 增加 `--generate-config` 参数（一键生成配置模板 JSON）
- 优先级：CLI 显式参数 > JSON > 默认值
- `CLI_REFERENCE.md` — 完整 CLI 参考手册（9章：快速开始、5个子命令参数表、JSON配置格式、Python API接入示例）

## 全量代码审查 (2026-06-01 20:23)

### 运行验证
- `import fl_space` ✅ 通过
- `python tests\test_quick.py` ✅ 全部通过
- Ruff lint 检查 ✅ 0 错误
- Kepler/Skyfield 双后端模拟 ✅ 正常

### 发现的 Bug（4 个）
1. **严重**: `OrbitSimulator.summary()` 在直接传入 `orbits`（非 constellation_config）时崩溃 — `self.constellation_config` 为 None 但第520行直接访问 `.distribution` 属性
2. **中等**: `CelestialBody.from_dict()` 不过滤多余字段，与 `GroundStation.from_dict()` 行为不一致
3. **中等**: `cli.py:_cmd_train` 中 `algo` 变量来源不一致（`--config` 时从 JSON 取，否则从 CLI 取），但导出 JSON 始终用 `args.algo`
4. **低**: `generate_walker_phases` 直接调用时 `sats_per_plane` 可能为 0（`ConstellationConfig` 有拦截但直接调用无保护）

### 用户自定义能力评估
- **宇宙环境自定义**: ✅ 完善 — CelestialBody 实例化/from_dict/from_name（含精确参数）、3种大气模型、GroundStationNetwork JSON导入导出
- **卫星行为自定义**: ✅ 完善 — MultiClusterConfig（多星簇）、SatelliteSpec（单星）、3种注册方式（装饰器/函数/字典）、预构建轨道直接传入
- **FL算法自定义**: ✅ 完善 — 4组件接口可替换、register_model() 注册自定义模型

### 扩展性预留
- 大气模型可扩展、通信参数已预留字段（tx_power_dbm, comm_range_km, antenna_gain_dbi）
- 多普勒频移/轨道摄动/3D可视化等已标注"未来扩展"
- Checkpoint机制、数据集集成在 MEMORY.md Step 4-7 计划中

## 4 个 Bug 修复完成 (2026-06-01 20:35)

### 修复详情（由易到难）
1. **Bug 2**: `celestial_body.py:294-296` — `from_dict()` 加字段过滤 `{k:v for k,v in d.items() if k in cls.__dataclass_fields__}`，与 GroundStation 对齐
2. **Bug 4**: `satellite_phases.py:45-46` — `generate_walker_phases` 入口加 `if num_planes > num_satellites: raise ValueError` 校验
3. **Bug 3**: `cli.py:388` — 导出 JSON 的 `"algorithm": args.algo` 改为 `"algorithm": algo`（使用局部变量而非 argparse 默认值）
4. **Bug 1**: `orbit_simulator.py:520` — `summary()` 的分布策略改为 `self.constellation_config.distribution if self.constellation_config else '自定义轨道'`

### 验证结果
- Ruff lint: 0 errors（4个修改文件均通过）
- `python tests\test_quick.py`: ✅ 全部通过
- 自定义轨道 + summary() 不崩溃
- from_dict 多余字段不报错
- Walker 参数越界抛出 ValueError

## 自定义环境 + FedProx 集成测试 (2026-06-01 20:44)

### 测试配置
- 自定义天体: Custom-400 (R=400km, GM=800 km^3/s^2, 自转12h, 无大气)
- 星座: 5卫星/1星簇, 轨道高度80km, 倾角60°
- 地面站: 5个 (全球分布)
- FL: FedProx (μ=0.01), 10轮, MNIST, IID, CPU

### 测试结果
- 轨道模拟: Kepler后端, 240 timeslots (4h@1min), 接触率 52.83%
- 通信窗口: 每颗卫星 20-22 个窗口, TS120 仅1颗卫星可见
- FL训练: 耗时99.4s, 准确率 89.58% → 95.87%, 损失 354.83 → 52.90
- 全部模块正常: 自定义天体 + 星簇 + 地面站 + 通信调度 + FedProx 端到端通过

## Bug 5: 训练损失未按 batch 数归一化 (2026-06-01 20:56)

- `fedprox.py:144`: `epoch_loss += ce_loss.item()` 累加的是 batch 均值，未除以 batch 数
- `fedavg.py` 的 `FixedEpochTrainer` 有同样问题
- 导致显示 `loss=354.83` 实际是 `batch均值之和/epoch数`，真实每样本 CE 约 0.95
- 不影响训练，但监控曲线失去可读性

## 虚拟时间追踪 (2026-06-01 20:58-21:10)

### FLRoundResult 加 timeslot 字段
- `core.py`: `FLRoundResult.timeslot: int = 0`
- `server.py`: `run_sync`/`run_async` 传递 timeslot，逐轮输出 `TS=开始→结束`
- `runner.py`: 完成摘要显示虚拟时间范围
- `get_history_dict()` 包含 timeslot 字段

### run_sync 改为通信窗口驱动（非固定timeslot跳跃）
- 旧: `timeslot = rnd * 10`（固定step）
- 新: `_advance_to_next_contact()` 找到下一次有卫星连接的时隙 → 分发模型 → 训练(不占时间) → `get_next_contact()` 等待返回窗口 → 聚合 → 时间跳到聚合后
- v2 对比: 当前 MLP [784,128,64,10] 10轮到 92%+ vs v2 50轮到 59.96%，差异原因可能是 v2 用更小模型或不同数据集，而非通信模型问题
