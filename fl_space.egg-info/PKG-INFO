Metadata-Version: 2.4
Name: fl-space
Version: 0.1.0
Summary: Space Federated Learning Research Framework — 模块化太空联邦学习研究框架
Author: FL Space Team
License: MIT
Requires-Python: >=3.9
Description-Content-Type: text/markdown
Requires-Dist: numpy>=1.24.0
Requires-Dist: matplotlib>=3.7.0
Provides-Extra: skyfield
Requires-Dist: skyfield>=1.48; extra == "skyfield"
Provides-Extra: full
Requires-Dist: torch>=2.0.0; extra == "full"
Requires-Dist: torchvision>=0.15.0; extra == "full"
Requires-Dist: seaborn>=0.12.0; extra == "full"
Requires-Dist: tqdm>=4.65.0; extra == "full"
Requires-Dist: skyfield>=1.48; extra == "full"

# SpaceFL — 太空联邦学习研究框架

[![Python](https://img.shields.io/badge/Python-3.9%2B-blue)](https://python.org)
[![License](https://img.shields.io/badge/License-MIT-green)](LICENSE)
[![Ruff](https://img.shields.io/badge/Linting-Ruff-ok)](https://github.com/astral-sh/ruff)

模块化、可扩展的开源框架，用于研究太空联邦学习中的轨道环境模拟、卫星通信与FL算法。

---

## 架构

```
fl_space/
├── environment/   # 环境模拟层 — 天体、大气、地面站、坐标工具
├── orbit/         # 轨道力学层 — Kepler/SGP4 双后端、可见性判断
├── simulator/     # 模拟器层 — 接触矩阵、主模拟引擎
└── config/        # 配置层 — 预设参数、JSON 配置加载
```

**双后端设计：**
| 后端 | 精度 | 依赖 | 适用场景 |
|------|------|------|---------|
| `kepler` (默认) | 二体近似 | 仅 numpy | 快速原型、概念验证 |
| `skyfield` | SGP4 + JPL DE421 | `pip install skyfield` | 高精度研究、真实任务规划 |

---

## 快速开始

```bash
pip install fl-space           # 核心安装
pip install fl-space[skyfield] # 含 Skyfield 高精度后端
```

### 5 分钟示例

```python
from fl_space.environment import CelestialBody, create_default_network
from fl_space.orbit import KeplerOrbit, create_circular_orbit
from fl_space.simulator import OrbitSimulator

# 创建地球 + 3颗卫星 + 7个地面站
earth = CelestialBody.earth()
gss = create_default_network(7)
orbits = [create_circular_orbit(500, 53, 0, i * 120, earth) for i in range(3)]

sim = OrbitSimulator(
    body=earth,
    orbits=orbits,
    ground_stations=gss,
    duration_hours=24,
)

# 查看通信记录
print(f"接触率: {sim.contact_rate:.1%}")
print(sim.get_communication_record(0))  # SAT-0 的完整通信记录
```

### 切换到高精度模式

```python
sim = OrbitSimulator(
    body=earth,
    orbits=orbits,
    ground_stations=gss,
    duration_hours=24,
    backend="skyfield",  # 启用 SGP4/JPL 高精度
    sim_start_date=(2025, 1, 1),
)
```

---

## 模块 API

### `fl_space.environment`
| 类/函数 | 说明 |
|---------|------|
| `CelestialBody` | 可配置天体参数，内置6个预设 (earth/mars/moon/jupiter/saturn/venus) |
| `CelestialBody.earth(precise=True)` | 从 JPL DE421 获取高精度参数 |
| `AtmosphereModel` | 大气模型基类 |
| `NoAtmosphere` / `ConstantHeightAtmosphere` / `ExponentialAtmosphere` | 三种大气模型 |
| `GroundStation` | 单个地面站 (经纬度/海拔/最小仰角/天线增益) |
| `GroundStationNetwork` | 地面站网络容器 |
| `create_default_network(n)` | 创建默认 n 站网络 |
| `coordinate_utils` | Haversine/角距/可见半角等工具函数 |

### `fl_space.orbit`
| 类/函数 | 说明 |
|---------|------|
| `KeplerOrbit` | Kepler 二体问题轨道计算器 |
| `OrbitalElements` | 轨道六要素 dataclass |
| `create_circular_orbit()` / `create_polar_orbit()` | 工厂函数 |
| `ConstellationConfig` | 星座配置 (Walker/Cluster/Uniform) |
| `generate_orbits()` | 按配置生成轨道 |
| `VisibilityEngine` | 单星可见性引擎 |
| `MultiSatVisibility` | 多星批量可见性 |
| `SkyfieldOrbitBackend` | Skyfield SGP4 高精度后端 |
| `get_precise_body_params()` | 快速获取 JPL 精确天体参数 |

### `fl_space.simulator`
| 类/函数 | 说明 |
|---------|------|
| `OrbitSimulator` | 主模拟器，整合所有层 |
| `create_default_simulator()` | 地球默认配置工厂 |
| `create_mars_simulator()` | 火星场景工厂 |
| `ContactMatrix` | 接触矩阵 (兼容/完整双模式) |

---

## 自定义配置

```python
from fl_space.config import load_sim_config_from_dict, EXPERIMENT_CONFIGS

# 从预设或自定义字典加载
config = {
    "body": {"name": "Earth", "radius_km": 6371, "GM": 398600.4},
    "constellation": {"type": "walker", "num_sats": 20, "num_planes": 5},
    "ground_stations": [...],
    "duration_hours": 24,
}
params = load_sim_config_from_dict(config)
sim = OrbitSimulator(**params)
```

---

## 代码规范

本项目使用 [Ruff](https://github.com/astral-sh/ruff) 进行代码规范检查：

```bash
ruff check fl_space/   # 检查
ruff check --fix       # 自动修复
ruff format fl_space/  # 格式化
```

---

## 开发状态

- [x] Step 1: 环境模拟设置 (Kepler + Skyfield 双后端)
- [ ] Step 2: 卫星设置 (行为模式、通信方式)
- [ ] Step 3: FL 算法选择
- [ ] Step 4: 数据集集成
- [ ] Step 5: 地面基站设置增强
- [ ] Step 6: Checkpoint 机制
- [ ] Step 7: 信息展示增强

---

## 许可证

MIT License — 详见 [LICENSE](LICENSE)
